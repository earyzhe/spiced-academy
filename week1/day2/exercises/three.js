// Write a while or for loop that counts down from 10 to 1, logging each number to the console.

var x = 10;

function tenToOne() {
    while (x > 0) {
        console.log(x);
        x--;
    }
}

tenToOne();
